---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: prometheus
  namespace: monitoring
spec:
  chart:
    spec:
      chart: ./otel/prometheus
      reconcileStrategy: Revision
      sourceRef:
        kind: GitRepository
        name: monitoring-source
  interval: 2m0s