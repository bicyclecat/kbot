# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
# Dummy comment added

replicaCount: 1
image:
  registry: "docker.io"
  repository: "bicyclecat"
  tag: "1.0.5-525e9a7"
  os: linux
  arch: amd64
podAnnotations:
  sidecar.opentelemetry.io/inject: "true"
secret:
  env: "TELE_TOKEN"
  name: "kbot"
  key: "token"
securityContext:
  privileged: true
env:
  metricsHost: "otel-collector:4317"
