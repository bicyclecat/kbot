# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
# Dummy comment addded
replicaCount: 1
image:
  registry: "ghcr.io"
  repository: "bicyclecat/kbot"
  tag: "v1.0.6-749006f"
  os: linux
  arch: amd64
secret:
  env: "TELE_TOKEN"
  name: "kbot"
  key: "token"
securityContext:
  privileged: true
